apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.fullname" . }}-checkers
  labels:
    {{- include "chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.checkers.service.type }}
  ports:
    - port: {{ .Values.checkers.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart.selectorLabels" . | nindent 4 }}
